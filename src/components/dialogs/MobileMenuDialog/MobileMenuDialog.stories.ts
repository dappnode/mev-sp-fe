import { MobileMenuDialog } from '.'
import type { Meta, StoryObj } from '@storybook/react'

const meta: Meta<typeof MobileMenuDialog> = {
  title: 'dialogs/MobileMenuDialog',
  component: MobileMenuDialog,
}

export default meta
type Story = StoryObj<typeof MobileMenuDialog>

export const Primary: Story = {}
