import { SubscribeToMevDialog } from '.'
import type { Meta, StoryObj } from '@storybook/react'

const meta: Meta<typeof SubscribeToMevDialog> = {
  title: 'dialogs/SubscribeToMevDialog',
  component: SubscribeToMevDialog,
}

export default meta
type Story = StoryObj<typeof SubscribeToMevDialog>

export const Primary: Story = {}
