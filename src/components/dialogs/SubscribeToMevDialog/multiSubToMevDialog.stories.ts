import { MultiSubscribeToMevDialog } from '.'
import type { Meta, StoryObj } from '@storybook/react'

const meta: Meta<typeof MultiSubscribeToMevDialog> = {
  title: 'dialogs/multiSubscribeToMevDialog',
  component: MultiSubscribeToMevDialog,
}

export default meta
type Story = StoryObj<typeof MultiSubscribeToMevDialog>

export const Primary: Story = {}
