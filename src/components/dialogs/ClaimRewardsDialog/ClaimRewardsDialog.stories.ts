import { ClaimRewardsDialog } from '.'
import type { Meta, StoryObj } from '@storybook/react'

const meta: Meta<typeof ClaimRewardsDialog> = {
  title: 'dialogs/ClaimRewardsDialog',
  component: ClaimRewardsDialog,
}

export default meta
type Story = StoryObj<typeof ClaimRewardsDialog>

export const Primary: Story = {}
