import { UnsubscribeToMevDialog } from '.'
import type { Meta, StoryObj } from '@storybook/react'

const meta: Meta<typeof UnsubscribeToMevDialog> = {
  title: 'dialogs/UnsubscribeToMevDialog',
  component: UnsubscribeToMevDialog,
}

export default meta
type Story = StoryObj<typeof UnsubscribeToMevDialog>

export const Primary: Story = {}
